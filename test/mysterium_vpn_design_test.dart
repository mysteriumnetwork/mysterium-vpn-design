// Widget tests live in test/widgets/. This file is kept so `flutter test`
// discovers at least one test target when run without arguments.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('package can be imported', () {
    // Intentionally blank — real tests are in test/widgets/.
    expect(true, isTrue);
  });
}
